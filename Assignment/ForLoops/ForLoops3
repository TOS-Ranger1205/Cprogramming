Question: Write a Program to Count Number of Digits in an Integer

Enter Number : 4567
Count Number : total Digits : 4 



Answer:

#include <stdio.h>  
int main()  
{  
   int n;  // variable declaration  
   int count=0;   // variable declaration  
   printf("Enter a number");  
   scanf("%d",&n);  
   while(n!=0)  
   {  
       n=n/10;  
       count++;  
   }  
     
   printf("\nThe number of digits in an integer is : %d",count);  
    return 0;  
}  



************************************************************


#include <stdio.h>
int main()
{
    int n;
     int count = 0;
     printf("Enter the number : ");
     scanf("%d", &n);
     
    for(; n > 0; )
     {
          n = n / 10;
          count = count + 1;  //count digits
     }
     printf("Number of digits : %d",  count);
     return 0;
}