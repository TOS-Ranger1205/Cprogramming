#include <stdio.h>

int main()
{
	int rows;
	printf("Number of rows: ");
	scanf("%d", &rows);


	for (int a = 1; a <= rows; a++) {

		for (int c = 1; c < 2 * a; c++) {
			printf("%d ", a);
		}
		printf("\n");
	}
	return 0;
}